services:
  app:
    image: node:20-alpine
    container_name: ihs-2025-app
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    ports:
      - "5173:5173"
